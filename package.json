{
  "name": "axenya-pipeline-dashboard",
  "version": "6.0.0",
  "description": "Axenya Pipeline Dashboard — Web App (Vercel)",
  "author": "Axenya",
  "private": true,
  "scripts": {
    "start": "node scripts/dev.js",
    "test": "node scripts/test-delta-invariant.js 3002",
    "check": "node --check api/bdr-treble.js && node --check public/bdr-treble.js && node --check public/premium.js && node --check scripts/local-server.js && node -e \"JSON.parse(require('fs').readFileSync('vercel.json','utf8'))\" && node scripts/_check-inline-js.js public/bdr-treble.html && node scripts/check-semantic.js",
    "predeploy": "node scripts/preflight-deploy.js",
    "deploy": "vercel --prod --yes",
    "generate-credentials": "node scripts/generate-credentials.js"
  },
  "dependencies": {},
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
